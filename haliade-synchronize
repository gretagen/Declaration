#!/bin/bash
# haliade-synchronize — apply system configuration from configuration.lua
set -e

if [ "$EUID" -ne 0 ]; then
    echo "To synchronize your declarative configuration, you must run this with superuser privileges."
    exit 1
fi

exec lua /usr/lib/declaration/sync.lua "$@"
